READ ~/workspace/agent-scripts/AGENTS.MD BEFORE ANYTHING (skip if missing).

# cc-sdd Mobile Template
- Start by reading `CLAUDE.md` and project-specific specs.
- Keep `.kiro/` and `.claude/` intact for compatibility.
- Scripts: `scripts/committer`, `scripts/docs-list.ts` (requires `tsx`), `scripts/browser-tools.ts` (requires `tsx`), `scripts/nanobanana`, `scripts/shazam-song`.
